# TerraformWithGCPExample

create sa in gcp console
create secret in jenkins with base64 encoded sa key
gcloud config set project $PROJECT_ID
gsutil mb -c regional -l europe-west1 gs://esiemes-default-tfstate
